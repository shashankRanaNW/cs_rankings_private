import 'package:flutter/material.dart';

class Conf extends StatelessWidget {
  final String name;
  final String area;
  final String rank;
  final double pub;
  final double adj;
  const Conf({super.key, required this.name, required this.area, required this.rank, required this.pub, required this.adj});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
